'use babel';

import X86AssemblyHighlighterView from '../lib/x86-assembly-highlighter-view';

describe('X86AssemblyHighlighterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
